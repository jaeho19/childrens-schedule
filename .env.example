# JWT secret for session tokens
JWT_SECRET=your-secret-key-change-in-production

# HMAC-SHA256 hash of the PIN (using JWT_SECRET as key)
# Generate with: node -e "console.log(require('crypto').createHmac('sha256','YOUR_JWT_SECRET').update('YOUR_PIN').digest('hex'))"
AUTH_PIN_HASH=your-hmac-hash-here
