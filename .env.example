# JWT secret for session tokens
JWT_SECRET=your-secret-key-change-in-production

# Bcrypt hash of the PIN
# Generate with: node -e "require('bcryptjs').hash('YOUR_PIN', 10).then(console.log)"
AUTH_PIN_HASH=$2b$10$...your-hash-here
